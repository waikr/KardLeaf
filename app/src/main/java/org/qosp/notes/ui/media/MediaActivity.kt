package org.qosp.notes.ui.media

import androidx.appcompat.app.AppCompatActivity

/**
 * Sandbox stub of Quillpad's [MediaActivity].
 *
 * Only the [ATTACHMENT] extra constant is referenced by
 * [org.qosp.notes.ui.editor.EditorFragment]; the activity itself is never
 * launched in the sandbox (no attachments).
 */
class MediaActivity : AppCompatActivity() {
    companion object {
        const val ATTACHMENT = "ATTACHMENT"
    }
}
