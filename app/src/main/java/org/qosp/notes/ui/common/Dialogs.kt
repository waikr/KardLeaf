package org.qosp.notes.ui.common

import org.qosp.notes.data.model.Note

/**
 * Sandbox stub of Quillpad's `showMoveToNotebookDialog`.
 *
 * The original lists notebooks from `activityModel` and moves notes between
 * them. The sandbox has no notebooks, so this is a no-op. It exists so the
 * original [org.qosp.notes.ui.editor.EditorFragment] compiles unchanged.
 */
fun BaseFragment.showMoveToNotebookDialog(vararg notes: Note) {
    // Sandbox no-op: no notebook subsystem.
}
