package org.qosp.notes.ui.widget

import android.content.Context

/**
 * Sandbox stub of Quillpad's [WidgetUpdateHelper].
 *
 * The original refreshes app widgets after note changes. The sandbox has no
 * widgets, so this is a no-op. It only needs to compile.
 */
object WidgetUpdateHelper {
    fun updateAllWidgets(context: Context) {}
}
