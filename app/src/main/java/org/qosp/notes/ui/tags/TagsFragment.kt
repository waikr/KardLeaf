package org.qosp.notes.ui.tags

import androidx.fragment.app.Fragment

/**
 * Sandbox stub of Quillpad's [TagsFragment].
 *
 * Only exists so the `fragment_tags` navigation destination resolves. It is
 * never reached in the sandbox (the editor's top overflow menu does not
 * inflate, so `actionEditorToTags` is never triggered).
 */
class TagsFragment : Fragment()
